[gcode_macro $]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $"

[gcode_macro $$]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $$"

[gcode_macro $G]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $G"

[gcode_macro $I]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $I"

[gcode_macro $N]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $N"

[gcode_macro $C]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $C"

[gcode_macro $X]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $X"

[gcode_macro $H]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $H"

[gcode_macro $J]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $J"

[gcode_macro $RST]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $RST"

[gcode_macro $SLP]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL $SLP"

[gcode_macro ?]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL ?"

[gcode_macro ~]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL ~"

[gcode_macro !]
gcode:
  RESPOND PREFIX="info" MSG="KCNC - GRBL !"